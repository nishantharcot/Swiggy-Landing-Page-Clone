(
  document.onreadystatechange = (): void => {
    if (document.readyState === 'complete') {
      const model = {

      };
      const view = {

      };
      const controller = {

      };
    }
  }
)();
